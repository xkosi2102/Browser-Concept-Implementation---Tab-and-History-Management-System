# Tema 1 - Browser #
/* POPESCU Florin-Cosmin 313CD*/
Tema 1 - Este o implementare a unui browser web simplificat, care permite gestionarea tab-urilor și paginilor web. Proiectul include
funcționalități precum crearea și gestionarea stivelor de pagini, adăugarea și eliminarea tab-urilor, precum și navigarea între paginile web.

Fisierele principale:
tema1.h - contine structurile cerute si cele neceseare, am adaugat structura tabsList care era mentionata in cerinta
dar nu era definita explicit, doar (lista dublu-înlănțuită de tab-uri, care folosește o santinelă)
tema1.c - implementarea functiilor si comenzilor cerute 
// # ca inspiratie si ajutor am vizualizat implementarile de la curs si de la laboratoare
Makefile - inspirat din cele prezente la arhivele laboratoarelor

Comentariile din cod explica actiunea si pasii principali si verificarile de eroare 
Toate structurile si functiile sunt denumite sugestiv. fiecare structura a fost alocata dinamic
si eliberata la finalul programului.

In fisierul tema1.c au fost implementate :
- Crearea și eliberarea stivelor (createStack, push, pop, clearStack, freeStack), precum și a paginilor (createPage) și tab-urilor (createTab, freeTab).  
- Gestionarea listelor de tab-uri (createTabsList, addTab, removeTab), plus funcții utile de căutare (findTabNode).
- Inițializarea browser-ului (initBrowser), care începe cu un tab implicit (ID 0).

In functia main am folosit un vector de structuri de tipul page pentru a retine datele despre paginile web
pe care l-am alocat dinamic. Ea citește datele de intrare (numărul de pagini, paginile, numărul de operații și comenzile),
procesează fiecare comandă și la final eliberează memoria și închide fișierele.
Pentru fiecare comanda am implementat o functie care sa o rezolve, am folosit un if pentru a le procesa
si am apelat functiile corespunzatoare in functie de comanda citita.

La rularea checker-ului pe calculatorul propriu am obtinut 100/100
si am testat toate comenzile cerute in enuntul problemei.